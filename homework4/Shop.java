package homework4;

import org.w3c.dom.ls.LSOutput;

public class Shop {
    public static void main(String[] args) {


        String isEdekaOpen = "";
        String isReweOpen = "";
        boolean canBuy = (isEdekaOpen == isEdekaOpen) || (isReweOpen != isReweOpen) && (isReweOpen == isReweOpen) || (isEdekaOpen != isEdekaOpen);
        System.out.println("Я могу купить еду, это " + canBuy);

    }
}

